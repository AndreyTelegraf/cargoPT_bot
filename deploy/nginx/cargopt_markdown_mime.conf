# Include this file inside the cargopt.pt server block.
# Markdown discovery files should be readable text, not binary downloads.
location ~ \.md$ {
    default_type text/markdown;
    charset utf-8;
    try_files $uri =404;
}
